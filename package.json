{
  "name": "curvopt-monorepo",
  "version": "2.0.0",
  "description": "CurvOpt-LLM: Curvature-Guided LLM Optimization",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node ./frontend/server.js",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {}
}
