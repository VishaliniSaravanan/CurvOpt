# Production Environment (Render)
# Copy this to .env.production and update BACKEND_URL with your actual backend service URL

# API endpoint - use relative path since frontend server proxies to backend
VITE_API_URL=/api

# Backend service URL (set as environment variable on Render dashboard)
# Key: BACKEND_URL
# Value: https://your-backend-service.onrender.com
